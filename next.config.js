/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  devIndicators: {
    buildActivity: false, // removes the bottom-left Next.js logo
  },
};

module.exports = nextConfig;
